/**
 * Exchanges two values.
 * @param x entries
 * @param k a key
 * @param l another key
 */
function swap<K, V>(x: Iterable<[K, V]>, k: K, l: K): IterableIterator<[K, V]> {
  var vk, vl;
  for(var [i, v] of x) {
    
  }
}
export default swap;
